# Plan A/B GPU training deps.
#
# No local venv could report installed versions when these were pinned, so
# these are KNOWN-GOOD versions for the GRPOConfig/SFTConfig args the scripts
# use (num_generations, max_completion_length, save_steps, save_total_limit,
# gradient_accumulation_steps, seed) - the trl 0.11.x era. Each run's
# run_manifest.json records the ACTUAL pip freeze; that is authoritative.
torch==2.4.0
transformers==4.44.2
trl==0.11.4
peft==0.12.0
datasets==2.21.0
accelerate==0.33.0
# CPU-only smoke of reward/label/env logic needs none of the above.
