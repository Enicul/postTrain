# Plan A/B GPU training deps (pin at pull time; tested shapes noted)
torch>=2.3
transformers>=4.44
trl>=0.11
peft>=0.12
datasets>=2.20
accelerate>=0.33
# CPU-only smoke of reward/label/env logic needs none of the above.
